apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui
  namespace: mlops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ui
  template:
    metadata:
      labels:
        app: ui
    spec:
      containers:
      - name: ui
        image: loksaieta/ui:latest
        ports:
        - containerPort: 8501
